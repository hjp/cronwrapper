* Smarter ok/fail mechanism: Patterns which don't signify failure on
  stderr, and patterns which do signify failure on stdout. Possibly
  ignore return code, too?
* Config file instead of command line args
